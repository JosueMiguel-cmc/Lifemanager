<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LifeManager"
        tools:targetApi="31">
        <activity android:name=".VIEW.activity_notas_view"
            android:exported="false"/>
        <activity
            android:name=".activity_splash"
            android:exported="false" />
        <activity
            android:name=".activity_calendario_view"
            android:exported="false" />
        <activity
            android:name=".activity_configuracoes_view"
            android:exported="false"
            android:label="@string/title_activity_configuracoes_view" />
        <activity
            android:name=".VIEW.PrincipalView"
            android:exported="false" />
        <activity
            android:name=".VIEW.Logview"
            android:exported="false" />
        <activity
            android:name=".VIEW.Cadview"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>